#INPUT()
#We use print() function to display something to users, Likewise we use input() function to get something from user.
#It is the built-in function that stops your programm to get user input.

# input("Enter your Name:") example 

#Variables & INPUT()
 
name = input("Enter your Name:") # here "name" is Dynamic value which means data entered by the user that can vary each time the program runs.
Age = 27 # here "age" is static value or hard coded value - fixed peice of data wrritten directly into your code that never changes at runtime.
print(name, "my age is", Age)